# Secrets / local MCP credentials — never commit
.env
.env.*
!.env.example

# Local MCP server state
.chroma/
.memory/

# Build-from-source MCP servers (cloned locally, not vendored)
mcp-servers/

# Python
__pycache__/
*.py[cod]
